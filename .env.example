DATABASE_URI=postgres://postgres:postgres@localhost:5435/speechless

MAILJET_API_KEY=
MAILJET_SECRET_KEY=
NEXT_PUBLIC_IS_VERIFICATION_ENABLED=true

BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=

# Must match the real deployed origin in production (e.g. https://detoast.nl).
# Used for Better Auth's trustedOrigins and every link built into emails.
# If unset in production, login/signup will silently fail.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Required for /api/cron/cleanup endpoint authentication
CRON_SECRET=